{% extends 'base.html.twig' %}

{% block title %}Guardian{% endblock %}

{% block body %}
    <h1>Guardian</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ guardian.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ guardian.name }}</td>
            </tr>
            <tr>
                <th>Surname</th>
                <td>{{ guardian.surname }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ guardian.email }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ guardian.telephone }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ guardian.adresse }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ guardian.city }}</td>
            </tr>
            <tr>
                <th>Codepostal</th>
                <td>{{ guardian.codepostal }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_guardian_index') }}">back to list</a>

    <a href="{{ path('app_guardian_edit', {'id': guardian.id}) }}">edit</a>

    {{ include('guardian/_delete_form.html.twig') }}
{% endblock %}
